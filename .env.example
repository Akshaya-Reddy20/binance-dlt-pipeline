# --- General ---
ENV=dev
BASE_URL=https://api.binance.com

# Comma-separated list of symbols
SYMBOLS=BTCUSDT,ETHUSDT

# Kline interval (Binance valid values: 1m,3m,5m,15m,30m,1h,2h,4h,6h,8h,12h,1d,3d,1w,1M)
INTERVAL=1h

# Optional: seed the first run (UTC ISO8601 or ms epoch). If empty, we start ~30 days back.
START_TIME=

# Destination: duckdb (local) OR clickhouse (future)
DESTINATION=duckdb
DUCKDB_DATABASE=./binance.duckdb

# If you later switch to clickhouse:
# DESTINATION=clickhouse
# CLICKHOUSE_HOST=localhost
# CLICKHOUSE_PORT=8123
# CLICKHOUSE_DATABASE=binance
# CLICKHOUSE_USER=default
# CLICKHOUSE_PASSWORD=